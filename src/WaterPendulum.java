public class WaterPendulum extends Ride {
    public WaterPendulum(String rideName, int amount, int startTime, int endTime, boolean adult, boolean children, boolean senior, TicketCounter ticketCounter, Operator operator, boolean isConfigure) {
        super(rideName, amount, startTime, endTime, adult, children, senior, ticketCounter, operator, isConfigure);
    }
}
